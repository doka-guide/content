---
title: "`@layer`"
description: "Управляем каскадными слоями с помощью чистого CSS."
authors:
  - tatianafokina
keywords:
  - ＠-правило
  - поддержка свойств
  - feature queries
  - CSS at-rule
  - cascade layers
  - layer ordering
  - CSS layers
  - CSS-слои
related:
  - css/has
  - css/where
  - css/import
tags:
  - doka
---

## Кратко

Директива `@layer` позволяет управлять каскадными слоями — способом группировки и управления порядком применения стилей в одном источнике. Благодаря таким слоям браузеры решают, какие стили в итоге применить на сайте.

Источник в CSS — место, откуда браузер берёт CSS-правила. Это могут быть ваши стили на сайте, настройки пользователей в браузере и системе, а также браузерные стили по умолчанию. Подробнее об этой и других концепциях узнаете из статьи «[Принцип каскада](/css/cascade/)».

С помощью `@layer` вы напрямую управляете слоями с разными наборами CSS-правил и решаете, какие стили специфичнее и в каком порядке их применять. Так что `@layer` пригодится для:

- сброса браузерных стилей по умолчанию;
- перезаписи стилей из CSS-фреймворков и библиотек;
- стилизации компонентов;
- разных состояний интерактивных элементов;
- цветовых тем, схем и поддержки разных режимов передачи цветов в системе.

<aside>

⚠️ Следите за размером итогового файла со стилями, если используете `@layer`. С этой директивой легко на выходе получить файл с большим количеством CSS-правил, которые перезаписыват друг друга.

</aside>

## Пример

Стили с `@layer` для состоянии кнопки при фокусе и когда она нективна. В слое `states` устанавливаем стили для `:disabled` и `:focus-visible`, а в `components` — стили кнопки по умолчанию.

```css
@layer components, states;

@layer components {
  .button {
    display: block;
    min-width: 210px;
    border: 2px solid transparent;
    border-radius: 6px;
    padding: 9px 15px;
    color: #000000;
    font-size: 18px;
    font-weight: 300;
    font-family: inherit;
    transition: background-color 0.2s linear;
  }

  .button:hover {
    background-color: #FFFFFF;
    cursor: pointer;
    transition: background-color 0.2s linear;
  }
}

@layer states {
  :disabled {
    background-color: #DDDDDD;
  }

  :focus-visible {
    border: 2px solid #FFFFFF;
    outline: none;
  }
}
```

<iframe title="Кнопка с разными состояниями" src="demos/button-states/" height="250"></iframe>

Директиву так же можно использовать для управлениями цветовыми темами, схемами и различными режимами передачи цветов. В этом примере определяем стили для светлой и тёмной темы.

```css
@layer theme {
  @layer light, dark;

  @layer light {
    body {
      --background: #FFFFFF;
      --color: #000000;

      background-color: var(--background);
      color: var(--color);
    }
  }
}

@media (prefers-color-scheme: dark) {
  @layer theme.dark {
    body {
      --background: #000000;
      --color: #FFFFFF;
    }
  }
}
```

## Как пишется

Есть несколько способов использования директивы `@layer`. В первую очередь слои могут быть назваными и анонимными, без имён.

С помощью названных слоёв напрямую управляете порядком стилей. При этом есть возможность управлять их порядком в одном месте.

Анонимные слои полезны, если важно использовать правила из одного слоя повторно, для нескольких элементов, а ещё не хотите, чтобы другие разработчики могли ими манипулировать.

### Названные слои

Укажите имя слоя после директивы `@layer`, а внутри перечислите нужные правила. Имя слоя может быть любым на латинице. К примеру, `project`, `framework`, `base`, `utilities` или `theme`.

```css
@layer any-layer-name {
  /* Одно или несколько CSS-правил */
}
```

Когда правила не вложены внутрь `@layer`, они побеждают. Например, здесь текст будет чёрным, несмотря на правила для класса `.text--pink` в `@layer`.

```html
<p class="text--pink">
  Хроносинкластический инфундибулум!
</p>
```

```css
/* Я проиграл */
@layer base {
  .text--pink {
    color: #F498AD;
  }
}

/* Я победил 🥇 */
p {
  color: #000000;
}
```

<iframe title="Вычисление стилей вне @layer" src="demos/layer-vs-unleared-rules/" height="250"></iframe>

Отдельное правило применится даже если расположено выше правил в слое, так что текст всё ещё остаётся чёрным.

```css
/* Я победил 🥇 */
p {
  color: #000000;
}

/* Я проиграл */
@layer base {
  .text--pink {
    color: #F498AD;
  }
}
```

Чтобы управлять несколькими слоями и их порядком в одном месте в файле со стилями, перечислите их через запятую после директивы `@layer`. Порядок слоёв имеет значение и влияет на специфичность CSS-правил. Первый слой из перечисленных перезаписывается слоями, которые расположены после него.

В примере текст в параграфе `<p>` будет чёрным (`#000000`), так как слой `layer-2` находится после `layer-1` и, соответстственно, выше в порядке представления.

```css
@layer layer-1, layer-2;

/* Я проиграл */
@layer layer-1 {
  p {
    color: #FFFFFF;
  }
}

/* Я победил 🥇 */
@layer layer-2 {
  p {
    color: #000000;
  }
}
```

Даже если мы повысим специфичность параграфа из первого слоя `layer-1`, добавив к нему, например, `id`, `layer-2` всё равно будет располагаться выше и текст в параграфе останется чёрным.

```css
@layer layer-1, layer-2;

/* Я проиграл */
@layer layer-1 {
  p#light {
    color: #FFFFFF;
  }
}

/* Я победил 🥇 */
@layer layer-2 {
  p {
    color: #000000;
  }
}
```

Слои можно вкладывать в другие слои. В этом случае появляются родительские и дочерние слои.

```css
@layer framework {
  @layer base {
    p {
      margin-block: 0.75em;
    }
  }

  @layer theme {
    p {
      color: #000000;
    }
  }
}
```

Если хотите их перечислить, сначала укажите название родительского слоя, а через точку - вложенного внутрь родительского, дочернего. И, опять же, внутри родительского слоя победит тот, кто перечислен в начале файла последним.

```css
@layer framework.base, framework.theme;

@layer framework {
  /* Я проиграл */
  @layer base {
    p {
      color: #FFFFFF;
    }
  }

  /* Я победил */
  @layer theme {
    p {
      color: #000000;
    }
  }
}
```

Имена слоёв могут повторяться в случае, когда есть вложенные и невложенные слои. При этом их правила не будут одинаковыми или перезаписаны другим слоем с таким же названием. В этом примере у нас есть два разных слоя - `base` и `framework.base`.

```css
@layer base;

@layer framework {
  @layer base { ... }
}
```

Когда у вас в один слой вложено одновременно отдельное правило и целый слой, выигрывает первое. Такие правила ведут себя так же, как правила за пределами директивы. Так что если боитесь, что вложенные в другие слои перезапишут стили друг друга, просто вынесите основные правила за пределы директивы.

Бла-бла-бла.

```css
@layer typography {
  p {
    color: green;
  }

  @layer content;
}

@layer typography.content {
  p {
    color: blue;
  }
}
```

Внутри директивы можно вкладывать и другие директивы, кроме `@import`. Например, `@media`.

```css
@layer framework {
  html {
    color: #000000;
    background: #FFFFFF;
  }

  @media (prefers-color-scheme: dark) {
    html {
      color: #FFFFFF;
      background: #000000;
    }
  }
}
```

### Анонимные слои

В случае анонимные слоёв указывают только директиву `@layer` с вложенными внутрь правилами, и при, например, импорте ссылаются на них с помощью `layer`:

```css
@layer {
  /* Одно или несколько правил */
}

@import url(framework.css) layer;
```

В этом случае не сможете добавить стили позже, так как в лучае анонимных слоёв важно то, где они расположены в файле.

Порядок каскадных слоёв в этом случае определяется по порядку их объявления. Все остальные правила идут уже после тех, что объявлены внутри `@layer`, даже если у них один родительский слой.

Также можно использовать функцию `layer()`. Это может быть полезно, когда надо заменить стили из фреймворка на свом, однако это не очень хорощо делать с точки зрения производительности.

```css
@import url(bootstrap.css) layer;

@layer {
  /* */
}
```

В случае анонимных слоёв также можно обращаться к их дочерним слоям с именами. В этом случе у нас есть доступ к слою base, но не к анонимному родительскому. Он так и остётмя анонимным.

```css
@import url(bootstrap.css) layer(base);

@layer {
  @layer base {
    /* Нужные правила */
  }
}
```

Один анонимный слой может переопределять стили в нескольких ситуациях. Связывает несколько блоков со стилями с одним слоем. В примере стили из файлов headings.css и links.css соскадированы с одним и тем же слоем default:

```css
@import url(headings.css) layer(default);
@import url(links.css) layer(default);

@layer default {
  audio[controls] {
    display: block;
  }
}
```

Если нужно связать несколько слоёв с файлом, перечислите их в той же директиве @import через запятую в нужном порядке:

```css
@import url(framework.css) layer(bootstrap), layer(tailwind);
```

## Как понять

Слои похожи на слои в PhotoShop. В этой программе у фотографии может быть несколько слоёв с разными фильтрами и ретушью, но при этом они не смешиваются друг с другом, прозрачные и складываются в одну картинку.

Это решает проблему со стилями без изменения селекторов или увеличения их специфичности, а также избегать конфликтов между разными слоями и их стилями из-за их расположения в файле. Часто они возникают, когда в вашем проекте несколько источников стилей. Например, используете фреймворки и библиотеки.

Это полезно в случае больших проектов и когда используете сторонние стили, препроцессоры, фреймворки.
