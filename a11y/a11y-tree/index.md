---
title: "Дерево доступности"
description: ""
authors:
  - tatianafokina
keywords:
  - программа чтения с экрана
  - программа экранного доступа
  - screen reader
  - assistive technology
  - a11y tree
related:
  - a11y/chto-takoe-a11y
  - a11y/aria-intro
  - a11y/wcag
tags:
  - article
---

_Дерево доступности_ или _доступное дерево (accessibility tree)_ — это представление элементов документа в виде дерева на основе [DOM](/js/dom/) (Document Object Model). Благодаря ему вспомогательные технологии, например, [скринридеры](/a11y/screenreaders/), знают про содержимое страницы и что на ней происходит.

## Как создаётся

В дерево доступности попадает информация о том, как свёрстана страница. Больше всего на него влияет HTML и чуть меньше CSS и JavaScript. При этом дерево доступности не полностью копирует DOM-дерево. Например, в него не попадают скрытые элементы со свойствами [`display: none`](/css/display/), [`visibility: hidden`](/css/visibility/), [атрибутом `hidden`](/html/hidden/) или декоративные [`<div>`](/html/div/).

Вспомогательные технологии взаимодействуют с деревьями не напрямую, а через посредника — [Accessibility API](/a11y/screenreaders/#accessibility-api) операционной системы пользователя. Деревья строят и передают дальше браузеры. Реализация деревьев отличается от браузера к браузеру. Например, у одной роли могут быть разные названия, по разному вычисляться имена элементов и так далее. Также деревья могут выстраиваться браузерами только в тот момент, когда к ним обращается вспомогательная технология, или всегда висеть где-то на фоновом процессе.

Дерево доступности создаётся параллельно с [DOM-деревом](/js/dom/#iz-chego-sostoit-dom). Браузеры ждут, пока отрисуются раскладка и стили, а потом принимают решение о том, что забрать в дерево доступности. Кроме семантических тегов и текста, в деревья, к примеру, попадает содержимое псекдокласов [`::before`](/css/before/) и [`::after`](/css/after/) и ARIA-атрибуты.

Браузеры такие избирательные, так как пытаются экономить свои ресурсы, как можно быстрее отдавать дерево вспомогательным технологиям и не засоряять объявления скринридеров.

## Доступные объекты

Похоже на DOM-дерево, только состоит не из HTML-элементов, а из _доступных объектов (accessible object)_. Каждый объект представляет один элемент интерфейса и содержит самую важную информацию об элементе с точки зрения вспомогательных технологий:

- роль;
- имя;
- описание;
- свойства и состояния.

В большинство семантических тегов эта информация встроена по умолчанию, однако роли, имена и поведение элементов можно явно задавать и изменять с помощью _[ARIA-разметки](/a11y/aria-intro/) (Accessible Rich Internet Applications)_. Это вспомогательная техника для создания более доступного контента для скринридеров. Расширяет возможности HTML с помощью специальных атрибутов и ролей.

Одно из главных правил использования ARIA — стараться не использовать ARIA. Так что она пригодится, когда не хватает возможностей HTML. К примеру, для сложных интерактивных элементов — вкладок, выпадающих списков, модальных окон или оповещений об ошибках. В этом случает [ARIA-атрибуты](/a11y/aria-attrs/) и JavaScript сделают поведение 'интерактивного элемента понятным и предсказуемым.

Доступные обхекты попадают в дерево в случае, если произошло одно из двух событий:

- элемент, его содержимое, свойства и отношения с другими элементами на странице важны для вспомогательных технологий; 
- сработало доступное событие — пользователь зашёл на сайт со скринридером, экранной лупой и прочим.

Давайте теперь разберём все составляющие доступного объекта подробнее.

[Картинка]

### Роль

Описывает то, что это за элемент и можно ли с ним взаимодействовать. Бывают встроенные и явные роли.

Встроенные роли — это роли, которые есть у [семантических тегов](/html/#semantika). Например, у ссылки [`<a>`](/html/a/) с атрибутом `href` есть [роль `link`](/a11y/role-link/):

```html
<a href="/cozy-page/">Уютная страница</a>
```

Кстати, даже у [`<span>`](/html/span/) и `<div>` есть встроенная роль [`generic`](/a11y/role-generic/), но это не делает их семантическими 🤯

Явные роли задают с помощью ARIA. К примеру, лёгким движением руки `<span>` может стать ссылкой благодаря атрибуту `role="link"`, CSS и JavaScript.

```html
<span role="link">Уютная страница</span>
```

Однако лучше так не делать и использовать возможности HTML по максимуму. Как минимум вам не придётся писать скрипт, чтобы сделать `<span>` настоящей ссылкой со всеми нужными свойствами.

Подробнее с концепцией ролей познакомитесь в статье «[ARIA-роли](/a11y/aria-roles/)».

### Имя

Другое название — доступное имя (accessible name). Если просто, то это подпись к элементу. Имена — важная часть опыта пользователей, которые могут только слышать интерфейс. Помогает им понять, что это именно это за элемент и какая у него цель. Обычно озвучивается при фокусе.

Имя берётся из текстового содержимого тегов или атрибутов.

Когда у элемента вообще нет имени, вспомогательные технологии расскажут только о его роли. В этом случае пользователи смогут узнать только о том, что это какая-то кнопка.

```html
<button type="button"></button>
```

Вернёмся к примеру со ссылкой. В её случае именем становится текст между открывающим и закрывающим тегами — «Уютная страница».

```html
<a href="/cozy-page/">Уютная страница</a>
```

В полями всё ещё проще. Их имя берётся из тега [`<label>`](/html/label/), когда он связан с полем атрибутом `for`.

```html
<label for="pokemon">Любимый покемон:</label>
<input type="text" id="pokemon">
```

Имена могут браться и из ARIA-атрибутов [`aria-label`](/a11y/aria-label/) и [`aria-labelledby`](/a11y/aria-labelledby/). Например, у этой кнопки с иконкой внутри имя берётся из `aria-label` — «Сделать комплимент».

```html
<button aria-label="Сделать комплимент">
  <!-- Иконка -->
</button>
```

У способов предоставления имени есть разные особенности и приоритетность. Подробнее узнаете из спецификации [Accessible Name and Description Computation 1.1](https://www.w3.org/TR/accname-1.1/).

### Описание

Другие названия — доступное описание (accessible description) или вспомогательный текст (helper text). Это дополнение к имени, которое подробнее рассказывает об элементе или содержит подсказку к нему. Озвучивается после имени и роли, если выбрана такая настройка в скринридере.

На самом деле это необязательная, опциональная часть доступного объекта, которая помогает пользователям глубже разобраться в интерфейсе. Описания особенно полезны, когда на сайте сложные или нестандартные элементы.

Описания к элементам добавляются с помощью специальных ARIA-атрибутов — [`aria-describedby`](/a11y/aria-describedby/), [`aria-details`](/a11y/aria-details/) и [`aria-description`](/a11y/aria-description/).

Например, описание этой кнопки берётся из `<span>` с текстом, который связан с [`<button>`](/html/button/) с помощью `aria-describedby`:

```html
<button
  type="button"
  aria-describedby="hint"
>
  Выбрать тему
</button>

<span id="hint">
  Выберите любимый цвет, и он появится
  на фоне страницы.
</span>
```

### Свойства и состояния

Содержат детали о раскладке и возможных действиях с объектами. К примеру, выбран или нет сейчас элемент, связан ли с ним другой и так далее.

Во многие семантические теги, кроме роли, встроены ещё свойства и состояния. Например, у чекбоксов есть атрибут `checked`. Он помогает пользователям скринридеров понять, какой сейчас выбран чекбокс.

```html
<label for="potato">Люблю пиццу с ананасом:</label>
<input id="potato" type="checkbox" checked>
```

Свойствами и состояниями можно управлять также с помощью ARIA. Какие-то ARIA-атрибуты полностью дублируют атрибуты из HTML, как [`aria-checked`](/a11y/aria-checked/).

```html
<span id="label">Люблю пиццу с ананасом:</span>
<span role="checkbox" aria-labelledby="label" aria-checked="true"></span>
```

Есть и уникальные ARIA-атрибуты, которые делают уже существующие семантические теги чуть более доступными. Например, [`aria-expanded`](/a11y/aria-expanded/) сообщает о состоянии свёрнутости и развёрнутости у связанного с основным элементом контента.

```html
<button
  aria-expanded="false"
  aria-controls="list"
>
  Все товары
</button>
<ul id="list">
  <!-- Список ссылок -->
</ul>
```

Рассказываем подробнее про свойства и состояния в статье «[ARIA-атрибуты](/a11y/aria-attrs/)».

## Как отлаживать

В дерево доступности можно заглянуть через инструменты разработчика в браузерах.

<details>
  <summary>Chrome и браузеры на Chromium</summary>

  Вкладка «Доступность» (Accessibility) в основной вкладке «Элементы». Дополнительно можете включить full-page accessibility tree, отжав чекбокс в аккордеоне «Дерево доступности» (Accessibility Tree)

</details>

<details>
  <summary>Firefox</summary>

  Вкладка «Доступность» (Accessibility) в основной вкладке «Элементы» > Опция «Доступные фичи» (Accessibility features) > Accessibility Tree

</details>

<details>
  <summary>Safari</summary>

  Вкладка «Узел» (Node) в основной вкладке «Элементы» > аккордеон «Доступность» (Accessibility)

</details>

В Chrome и Firefox самые детальные превью деревьев. В этих браузерах можете найти целое дерево доступности и заглянуть в отдельные узлы.

Во вкладке «Доступность» обычно можно посмотреть дерево целиком, а также узнать подробнее про отдельный элемент: какие у него есть ARIA-атрибуты и вычисляемые свойства, к примеру, имя и роль.

[Картинка]

В Chrome и Firefox также можете выбрать, где и как отображать дерево доступности. Чтобы увидеть сразу всё дерево, отожмите экспериментальный чекбокс «Включить дерево доступности всей страницы». В этом случае рядом с HTML-кодом страницы появится переключатель с фигурой человека, который переключает между DOM-деревом и деревом доступности.

[Картинка]

Давайте посмотрим на дерево доступности, которое получается на основе разметки [главной страницы Доки](https://doka.guide/) в Chrome. Визуально дерево напоминает иерархию папок в системе, где вместо папок — узлы, а вместо названий папок — роли тегов. На скриншоте есть четыре больших узла с хедером `banner`, основной частью страницы `main`, футером `contentinfo` и диалоговым окном `dialog`. Внутри каждого крупного узла есть другие, поменьше. К примеру, внутри элемента хедера есть секция `section`, текстовое поле `textbox` и текстовый элемент с косой чертой.

[Картинка]

Давайте теперь посмотрим на отдельный доступный объект. Возьмём поле для почты из попапа с подпиской на рассылку от Доки.

- _Name_ — имя поля, взятое из `<label>`. Это «Оставьте вашу почту».
- _Role_ — встроенная роль поля `textbox`.
- _Invalid user entry: false_ — в поле пока нет неверных данных.
- _Focusable: true_ — элемент интерактивный и на нём можно сделать фокус с клавиатуры.
- _Editable: plaintext_ — в поле можно ввести текст.
- _Can set value: true_ — можно задать любое значения.
- _Multi-line: false_ — поле однострочное, в нём нельзя перейти на новую строку.
- _Read-only: false_ — поле активное, а не только для чтения.
- _Required: true_ — поле обязательное, так как на нём установлен атрибут `required`.
- _Labeled by_ — дополнительное описание, которое добавлено через `aria-labelledby`.

[Картинка]

Хотя браузеры дают заглянуть внутрь деревьев доступности, не стоит полностью полагаться на них, когда тестируете доступность интерфейса. В случае браузеров деревья — это абстракции, которые не всегда полностью повторяют _реальные_ деревья, к которым получают доступ вспомогательные технологии. Особенно тяжело деревья, которые видим в браузерах, справляются с обновлением информации. Например, когда динамически меняете название элемента или показываете и скрываете подсказку к нему.

Подробнее про работу с деревом доступности в разных браурерах:

- [Дерево доступности в инструментах разработчика Chrome](https://developer.chrome.com/blog/full-accessibility-tree/).
- [Документация про инспектор доступности в Firefox](https://firefox-source-docs.mozilla.org/devtools-user/accessibility_inspector/).
- [Документация про тестирование доступности в Safari](https://developer.apple.com/library/archive/documentation/Accessibility/Conceptual/AccessibilityMacOSX/OSXAXTestingApps.html#//apple_ref/doc/uid/TP40001078-CH210-TPXREF101).

## AOM

Пока мы не можем получить к доступному дереву прямой доступ, управлять им или читать его содержимое.

Сейчас отдельная группа разрабатывает документацию для API (Application Programming Interface, интерфейс программирования приложения), которое позволит разработчикам получать доступ к дереву доступности в браузере напрямую, вычисляемым ролям, именам и прочему, а также манипулировать им. Оно называется [Модель доступного объекта](https://wicg.github.io/aom/explainer.html) (Accessibility Object Model, коротко AOM).

##  Дополнительные ссылки

Если хотите глубоко разобраться в теме, рекомендуем пару исчерпывающих материалов:

- [Semantics to Screen Readers](https://alistapart.com/article/semantics-to-screen-readers/).
- [Accessibility APIs: A Key To Web Accessibility](https://www.smashingmagazine.com/2015/03/web-accessibility-with-accessibility-api/).
- [The Accessibility Tree](https://whatsock.com/training/).
