name: Deploy Platform With Content

on:
  schedule:
    - cron:  '00 5 * * *'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Ночная сборка сайта с контентом на Netlify
        run: curl -X POST -d {} ${{ secrets.NETLIFY_BUILD_HOOK }}
