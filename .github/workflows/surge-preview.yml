name: Surge

on:
  pull_request:

  workflow_dispatch:
    inputs:
      message:
        required: true
        description: Surge

  workflow_run:
    workflows:
      - YaSpeller
    branches-ignore:
      - main
    types:
      - completed

jobs:
  preview:
    runs-on: ubuntu-latest
    # if: ${{ github.event.workflow_run.event == 'pull_request' && github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Загрузка platform
        uses: actions/checkout@v2
        with:
          repository: doka-guide/platform
      - name: Загрузка контента
        uses: actions/checkout@v2
        with:
          path: content
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: Кэширование модулей
        uses: actions/cache@v2
        env:
          cache-name: cache-node-modules
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-
      - name: Установка модулей
        run: npm ci
      # - name: Сборка и публикация сайта из репозитория
      #   if: $ {{ secrets.GITHUB_TOKEN != "" }}
      #   uses: doka-guide/surge-preview@main
      #   id: preview_step
      #   with:
      #     surge_token: ${{ secrets.SURGE_TOKEN }}
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     dist: dist
      #     build: |
      #       node make-links.js --default
      #       npm run build
      - name: Преобразование строки
        run: echo "::set-env name=REPOSITORY_NAME::$(echo '$GITHUB_REPOSITORY' | awk -F / '{print $2}' | sed -e 's/:refs//')"
      - name: Сборка и публикация сайта
        # if: $ {{ secrets.GITHUB_TOKEN == "" }}
        run: |
          node make-links.js --default
          npm run build
          export DEPLOY_DOMAIN=`https://${{ github.repository_owner }}-${REPOSITORY_NAME}-${{ job.container.id }}-pr-${{ github.event.number }}.surge.sh`
          npx surge --project ./dist --domain $DEPLOY_DOMAIN --token ${{ secrets.SURGE_TOKEN }}
      # - name: Получение ссылки для превью
      #   run: echo "Ссылка на превью — ${{ steps.preview_step.outputs.preview_url }}"
