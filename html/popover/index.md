---
title: "Поповер"
description: "Нативный способ создавать всплывающие элементы."
authors:
  - solarrust
related:
  - html/dialog
  - html/button
  - a11y/role-tooltip
keyword:
  - Popover API
  - всплывающее окно
  - всплывающая подсказка
  - модальное окно
  - модалка
tags:
  - article
---

## Кратко

Удобный способ создавать всплываютщие элементы. Новая возможность HTML. Имеет много встроенных фишек типа закрытия по клику мимо элемента или по нажатию кнопки <kbd>Esc</kbd>.

Можно управлять как при помощи атрибутов HTML, так и через JavaScript.

## Пример

```html
<button
  popovertarget="my-popover"
  popovertargetaction="toggle"
>
  Всплываем!
</button>

<div id="my-popover" popover>
  <p>🛳️</p>
</div>
```

## Как понять

Долгое время в HTML не существовало простого способа реализовать всплывающие подсказки. Самодельные решения требовали большого внимания к мелочам и часто не учитывали всех сценариев использования. Теперь при помощи нескольких атрибутов можно создать правильную, удобную для всех всплывающую подсказку.

Преимущества:

- Всплытие на верхний уровень. Элемент всегда будет отображаться выше всех остальных. Больше не нужно возиться с [`z-index`](/css/z-index/)
- Простое закрытие. Клик за пределами элемента закроет его и вернёт фокус на последний активный элемент на странице.
- Фокус внутри всплывающей подсказки. При использовании клавиатуры следующий элемент, попавший в фокус, будет внутри всплывающей подсказки.
- Поддержка клаватуры. По нажатию на клавишу <kbd>Esc</kbd> всплывающая подсказка закроется.
- Семантическая связь. Привязка всплывающей подсказки к элементу, который её вызывает.

## Как пишется

### Обязяательные атрибуты

- `popover` — указывает, что блок теперь будет всплывающей подсказкой.
- `id` — уникальный идентификатор блока, используется для показа всплывающей подсказки.
- `popovertarget` — связывает элемент, кнопку или другой HTML-тег с всплывающей подсказкой. В качестве значения используется `id` элемента, который хотим показать.

```html
<button popovertarget="my-popover">
  Показать всплывающую подсказку
</button>

<div id="my-popover" popover>
  <p>Я — всплывающая подсказка с дополнительной информацией!</p>
</div>
```

<iframe title="Обязательные атрибуты" src="demos/required/" height="300"></iframe>

<aside>

🤫 Есть разговоры, что в дальнейшем атрибут `popovertarget` будет заменён на `invoketarget`. Но пока у него плохая браузерная поддержка. Используйте `popovertarget`.

</aside>

### Кнопка закрытия

По умолчанию элемент с `popovertarget` открывает и закрывает всплывающую подсказку. Это поведение можно изменить с помощью атрибута `popovertargetaction`.

Возможные значения:

- `toggle` — значение по умолчанию, применяется, если атрибут не прописан. Переключает состояние всплывающей подсказки между открытым и закрытым.
- `show` — открывает всплывающую подсказку.
- `hide` — закрывает всплывающую подсказку.

Таким образом можно создать отдельные кнопки для открытия и закрытия элемента.

В примере ниже кнопка «Показать всплывающую подсказку» будет только открывать всплывающую подсказку, но не закрывать её. Для закрытия элемента внутрь добавлена кнопка закрытия.

```html
<button popovertarget="my-popover" popovertargetaction="show">
  Показать всплывающую подсказку
</button>

<div id="my-popover" popover>
  <button popovertarget="my-popover" popovertargetaction="hide">
    <span aria-hidden="true">❌</span>
    <span class="sr-only">Закрыть</span>
  </button>

  <p>Я — всплывающая подсказка с дополнительной информацией!</p>
</div>
```

<iframe title="Кнопка закрытия" src="demos/targetaction/" height="300"></iframe>

### Значения `popover`

На самом деле у атрибута `popover` есть два значения:

- `auto` — значение по умолчанию, не требуется прописывать явно. При открытии всплывающей подсказки закроются все остальные всплывающие подсказки на странице. Всплывающая подсказка может быть закрыта по клику за пределами элемента.
- `manual` — другие всплывающие подсказки не закрываются. Всплывающая подсказка не может быть закрыта по клику за её пределами.

В демо ниже две всплывающие подсказки с разными значениями. Покликайте по ним, посмотрите на разницу в их поведении.

```html
<button popovertarget="popover-auto">
  <code>popover="auto"</code>
</button>

<div id="popover-auto" popover="auto">
  <p>Закрываю другие всплывашки. Легко закрываюсь.</p>
</div>

<button popovertarget="popover-manual">
  <code>popover="manual"</code>
</button>

<div id="popover-manual" popover="manual">
  <p>Не закрывают других. Без кнопки не закроюсь.</p>
</div>
```

<iframe title="Разница значений атрибута popover" src="demos/manual/" height="300"></iframe>

## Стилизация

Стили всплывающей подсказки по умолчанию:

```css
[popover] {
  position: fixed;
  width: fit-content;
  height: fit-content;
  inset: 0px;
  margin: auto;
  border: solid;
  padding: 0.25em;
}
```

Вы можете переопределять их на своё усмотрение.

Можно стилизовать подложку открытой всплывающей подсказки при помощи псевдокласса `::backdrop`.

```css
[popover]::backdrop {
  background-color: rgb(255 255 255 / 0.5)
}
```

<iframe title="Стилизация подложки" src="demos/backdrop/" height="300"></iframe>

## Анимации

Можно анимировать открытие и закрытие всплывающей подсказки. Для этого потребуется описать несколько состояний.

При помощи директивы `@starting-style` задаём стили, от которых будет начинаться анимация открытия:

```css
@starting-style {
  [popover]:popover-open {
    opacity: 0;
    rotate: 1turn;
  }
}
```

Затем прописываем стили для открытого элемента:

```css
[popover]:popover-open {
  opacity: 1;
  rotate: 0turn;
  transition: rotate .5s, opacity .5s, display .5s, overlay .5s;
}
```

И в конце пишем конечные стили при закрытии окна:

```css
[popover]:not(:popover-open) {
  scale: 0;
  transition: scale .3s, display .3s, overlay .3s;
}
```

Обратите внимание на свойство `overlay`, которое используется внутри [`transition`](/css/transition/). Это ключевое слово необходимо для анимации всплытия элемента в верхний слой.

<iframe title="Анимация всплывающей подсказки" src="demos/animation/" height="300"></iframe>

## Отличия от `<dialog>`

В чём отличие Popover API от тоже довольно нового [`<dialog>`](/html/dialog/)? Кажется, что оба создают всплывающие окна.

Сам по себе элемент с атрибутом `popover` не несёт никакой семантической нагрузки. Вы можете самостоятельно добавить ему нужного смысла при помощи [ARIA-ролей](/a11y/aria-roles/).

Диалог, открытый в режиме модального окна, не может быть закрыт по клику за пределами элемента. Всплывающая подсказка, напротив, поддерживает такую возможность.

Также модальное окно блокирует остальную страницу под ним. Всплывающее окно так не поступает.

## Методы JavaScript

Для большинства сценариев работы с всплывающими подсказками совсем не нужен JavaScript. Но если есть необходимость, то можно использовать следующие методы:

- `.showPopover()` — открыть всплывающую подсказку.
- `.hidePopover()` — закрыть всплывающую подсказку.
- `.togglePopover()` — переключить состояние всплывающей подсказки между открытым и закрытым.

```js
document.getElementById('my-popover').showPopover()
```

Проверить, что всплывающая подсказка открыта можно при помощи `.matches(':popover-open')`, `.checkVisibility()` или `event.newState === 'open'`.


