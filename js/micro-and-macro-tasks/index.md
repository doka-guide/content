---
title: Микро- и макротаски
description: "Понимание микротасок и макротасок — ключ к созданию оптимизированных и высокопроизводительных приложений."
authors:
  - marsel
related:
  - js/web-workers
  - js/fetch
  - js/loop
tags:
  - article
---

## Event loop

Вспомним, как функционирует Event loop (цикл событий) в браузере. Он состоит из трёх основных частей:

- Движок JavaScript — компонент, который выполняет код JavaScript и обеспечивает доступ к Web API, Стэку (Stack) и Куче (Heap).
- Стек (Stack) — это место, куда попадает наш код, выполняющийся по принципу LIFO (последним вошёл и первым вышел). Давайте посмотрим на примере:

```typescript
function first() {
  console.log('Выполнение первой функции')

  // Вызов второй функции, добавляется в стэк
  second() 
  console.log('Возврат в первую функцию после второй')
}

function second() {
  console.log('Выполнение второй функции')

  // Вызов третьей функции, добавляется в стэк
  third()
  console.log('Возврат во вторую функцию после третьей')
}

function third() {
  console.log('Выполнение третьей функции')
}

// Начало с первой функции
first()
```

- `Выполнение первой функции`.
- `Выполнение второй функции`.
- `Выполнение третьей функции`.
- `Возврат во вторую функцию после третьей`.
- `Возврат в первую функцию после второй`.

Стэк в движке JavaScript управляет порядком выполнения кода, а так же тем где будут хранится данные переменной: в куче если переменная ссылается на ссылочный тип данных, в стэке если переменная хранит примитив. Когда функция вызывает другую функцию, контекст первой добавляется в стэк, и выполнение передается следующей. Когда выполнение завершается, контекст извлекается из стэка, и код продолжает выполнение с предыдущей функции.

- Куча — это область памяти, в которой хранятся ссылочные типы данных.
- Web API — это интерфейсы браузера, такие как `fetch`, таймеры и другие функции.
- Очереди задач — место, где накапливаются задачи, соотвествующие различным событиям пользовательского интерфейса и событиям, инициированным Web API. Например, результат выполнения `fetch`.

![Основные участники Event loop](./images/runtime.png)

Event loop работает следующим образом:

1. **Выполнение задач/кода в стэке:** прежде всего, цикл событий ждёт, пока стэк вызовов полностью освободится от текущих функций и задач.
2. **Проверка очереди событий:** как только стэк вызовов опустошён, цикл событий начинает проверять очереди событий. В браузерах существует несколько очередей для разных типов задач, таких как макрозадачи и микрозадачи.
3. **Выбор задачи из очереди:** специальный механизм в браузере определяет, какую задачу из очереди следует выполнить следующей. В этом процессе учитывается тип задачи и её приоритет. Говоря выполнить следующей, имеется в виду переместить в стэк, где её обработает движок JavaScript.
  - **Выполнение микрозадач:** цикл событий обработает все микротаски до того, как перейдёт к следующей макрозадаче в очереди.
  - **Переход к следующей макрозадаче:** после того как все микротаски выполнены, цикл событий возвращается к выбору и запуску следующей макрозадачи из очереди. Стоит заметить, что, если в процессе выполнения макрозадачи возникают микротаски, они получают приоритет. Только после обработки всех микротасок будет взята в работу одна следующая макрозадача.
4. **Повторение цикла:** этот процесс повторяется циклически. Цикл событий постоянно мониторит стэк вызовов и очереди событий, обеспечивая непрерывное и эффективное выполнение задач.

Для большего понимания как это работает посмотрим на схему:

![Пример обработки задачи в Event loop](./images/event-loop-example.png)

1. Сначала мы выполняем запрос `fetch`.
1. Из стэка задача помещается в Web API.
1. Далее браузер выполняет запрос и возвращает результат выполнения в очередь в своём внутреннем представлении.
1. Далее Event loop помещает результат выполнения `fetch` в стэк, где его обрабатывает JavaScript-движок.

💡 Обратите внимание: Event loop не является частью движка JavaScript.

## Микротаски и макротаски

Перед тем, как разбираться с примерами кода, давайте поймём, что такое микро- и макротаска. _Микротаски (реже микрозадачи)_ — это отложенные задачи, которые имеют приоритет над макротасками.

Примеры микротасок:

- операции с промисами (например, `Promise.then()`, `Promise.catch()`, `fetch()` и так далее);
- операции с очередью мутации (например, используемые в API `MutationObserver` для наблюдения за изменениями DOM);
- операции, связанные с `queueMicrotask()`, — функцией для явного добавления микротасок.

_Макротаски (реже макрозадачи)_ — это тоже отложенные задачи, у которых меньший приоритет по сравнению с микротасками.

Примеры макротасок:

- обработка таймеров (`setTimeout`, `setInterval`);
- обработка событий пользовательского ввода (например, клики, скроллинг);
- выполнение AJAX-запросов.

## Асинхронность в браузере

Рассмотрим два вида асинхронного поведения, доступного в браузере.

**Асинхронность WebAPI**. Разберёмся, как будет работать `fetch`. Мы выполняем `fetch`-запрос, точнее, его выполняет движок JavaScript. Далее вызов передаётся на сторону Web API, а основной поток браузера продолжает работу. После того как `fetch`-запрос завершится, Web API вернёт результат выполнения в очередь. После результат выполнения подхватит Event loop и обработает JavaScript-движок.

**Асинхронность JavaScript-кода (псевдоасинхронность)**. Если возьмём функцию, которая выполняет цикл от 0 до 1 000 000 000, завернём её в промис, то как вы думаете, что произойдёт? Правильно, браузер зависнет. Почему? Дело в том, что, оборачивая код в промис, мы только просим браузер отложить выполнение этого кода на момент, пока очередь не будет обработана. Оборачивание синхронного кода в `Promise` или запуск его через `queueMickotask` помещает его _почти_ в конец очереди. Почему почти? С этим разберёмся чуть позже.

💡 Есть ли способ выполнить JavaScript-код асинхронно и в отдельном потоке браузера? Да! Это возможно с помощью [Web Worker API](/js/web-workers/).

## Примеры макротасок

Макротаски включают в себя задачи, связанные с таймерами (`setTimeout`, `setInterval`), и запланированные на выполнение одна за другой.

```javascript
console.log('Начало')

setTimeout(() => {
  console.log('Макротаска выполнена')
}, 0)

console.log('Конец')
```

В этом примере порядок вывода следующий:

1. `Начало`.
1. `Конец`.
1. `Макротаска выполнена`.

Даже если задержка `setTimeout` равна нулю миллисекунд, колбэк не выполняется немедленно. Он запланируется как макротаска и выполнится в конце приоритета выполнения.

## Примеры микротасок

Микротаски включают в себя промисы (promises) и обработчики, связанные с ними.

```javascript
console.log('Начало')

Promise.resolve().then(() => {
  console.log('Микротаска выполнена')
})

console.log('Конец')
```

Здесь порядок выполнения такой:

1. `Начало`.
1. `Конец`.
1. `Микротаска выполнена`.

Поскольку микротаски имеют более высокий приоритет, чем макротаски, колбэк из промиса выполняется сразу после завершения текущего цикла выполнения, но перед следующим циклом событий. Это работает таким образом, даже если в Event loop уже есть макротаски.

## Комбинированный пример: микро- плюс макротаска

Давайте посмотрим на комбинированный пример, который включает в себя и микротаски, и макротаски:

```javascript
console.log('Начало')

setTimeout(() => {
  console.log('Макротаска выполнена')
}, 0)

Promise.resolve().then(() => {
  console.log('Микротаска выполнена')
})

Promise.resolve().then(() => {
  console.log('Микротаска выполнена 2')
})

console.log('Конец')
```

Порядок выполнения будет следующим:

1. `Начало`.
1. `Конец`.
1. `Микротаска выполнена`.
1. `Микротаска выполнена 2`.
1. `Макротаска выполнена`.

Этот пример показывает, что микротаски (здесь промисы) выполняются перед макротасками (здесь таймеры), даже если они были запланированы в коде позже. Это подчёркивает их приоритетность в Event loop.

💡 Обратите внимание на приоритеты. Сначала будут выполнены **все** микротаски, а потом — **одна** макротаска.

## Пример с рендерингом страницы

Мы опустили детали, когда упомянули, что после всех микротасок выполняется одна макротаска. На самом деле, после микротасок может произойти рендеринг страницы. Если вы используете `requestAnimationFrame`, вызовы могут выполняться до того, как начнётся выполнение макротаски. Браузер старается вставить `requestAnimationFrame` в ближайший рендеринг. Все вызовы `requestAnimationFrame` происходят примерно каждые 16–67 миллисекунд, но **строго** после завершения всех микротасок. Давайте разберём на примере ниже:

```javascript
console.log('Синхронный старт')

// Макротаска с setTimeout
setTimeout(() => {
  console.log('Макротаска 1: setTimeout')
}, 0)

// Макротаска с setTimeout
setTimeout(() => {
  console.log('Макротаска 2: setTimeout')
}, 0)

// Микротаска с Promise
Promise.resolve()
  .then(() => {
    console.log('Микротаска 1: выполнение первого промиса')
  })
  .then(() => {
    console.log('Микротаска 2: выполнение второго промиса')
  })

requestAnimationFrame(() => {
  console.log('Первый requestAnimationFrame: обновление анимации')
})

requestAnimationFrame(() => {
  console.log('Второй requestAnimationFrame: обновление анимации')
})

console.log('Синхронный конец')
```

Порядок вывода:

1. `Синхронный старт`.
1. `Синхронный конец`.
1. `Микротаска 1: выполнение первого промиса`.
1. `Микротаска 2: выполнение второго промиса`.
1. `Первый requestAnimationFrame: обновление анимации`.
1. `Второй requestAnimationFrame: обновление анимации`.
1. `Макротаска 1: setTimeout`.
1. `Макротаска 2: setTimeout`.

Нам повезло, и rAF-вызовы выполнились перед макротасками. Если мы выполним этот код ещё раз, порядок rAF и микротаски могут поменяться местами.

## Пример макротасок, создающих микротаски

```javascript
console.log('Начало скрипта')

setTimeout(() => {
  console.log('Макротаска: setTimeout')

  // Микротаска, созданная внутри макротаски
  Promise.resolve().then(() => {
    console.log('Микротаска: обработка промиса внутри setTimeout')
  })

  Promise.resolve().then(() => {
    console.log('Микротаска: обработка промиса внутри setTimeout 2')
  })
}, 0)

setTimeout(() => {
   console.log('Макротаска: setTimeout 2')
}, 0)

Promise.resolve().then(() => {
  console.log('Микротаска: обработка первого промиса')
})

console.log('Конец скрипта')
```

Последовательность вывода будет следующей:

1. `Начало скрипта`.
1. `Конец скрипта`.
1. `Микротаска: обработка первого промиса`.
1. `Макротаска: setTimeout`.
1. `Микротаска: обработка промиса внутри setTimeout`.
1. `Микротаска: обработка промиса внутри setTimeout 2`.
1. `Макротаска: setTimeout 2`.

Обратите внимание на ситуацию, когда микротаска создаётся в макротаске. Пока макротаска не выполнит свой колбэк, браузер не узнает о том, что внутри есть микротаска. Если в списке задач есть макрозадачи, которые ожидают обработки, а в процессе выполнения одной из макрозадач появились микрозадачи, выполнение оставшихся макрозадач отложится. Это продлится до тех пор, пока все микрозадачи не завершатся, даже если они появились после макрозадач.

## Контексты цикла событий

В этом разделе рассмотрим изолированные контексты с собственные циклы обработки событий: основная страница браузера, iframe и Web worker. Каждый из элементов обладает уникальным циклом событий, который не пересекается с другими. С web worker всё довольно прямолинейно. Сначала задача отправляется в worker, и процесс обработки происходит независимо от основного потока браузера.

Ситуация с iframe сложнее. Хоть iframe и обладает собственным циклом событий, у него нет отдельного потока выполнения. Тем не менее это не означает, что основная вкладка браузера зависнет из-за затратной задачи. Зависнет только iframe, а вкладка продолжит работу, хотя браузер будет уведомлять пользователя о проблеме.

## Философия микро- и макротасок

До 2015 года стандарт не подразумевал микротаски, до 2014 не подразумевал и Web worker, а `requestAnimationFrame` появился только в 2011. Разработчикам приходилось довольствоваться асинхронностью, основанной на макротасках или Timer API. В итоге сетап был такой: ни макротасок, ни raf, ни Web worker, и никакого праздника.

Представим ситуацию, когда нужно посчитать что-то сложное на клиенте или, например, вы создаёте кастомные анимации, которые невозможно сделать через CSS. При этом окно браузера не должно блокироваться, а задача должна выполнялняться. В браузере нет многопоточности, есть только однопоточный Event loop. Как мы можем выйти из ситуации? Правильно, разбить задачу на много подзадачек и выполнить их, постепенно освобождая процесс для рендеринга, событий и запросов.

Рассмотрим пример:

```javascript
function processArrayInChunks(array, chunkProcessingTime) {
  let index = 0
  const startTime = Date.now()

  function processChunk() {
    const chunkStartTime = Date.now()

    while (index < array.length && (Date.now() chunkStartTime < chunkProcessingTime)) {
      // Пример обработки: увеличиваем каждый элемент массива на 1
      array[index] += 1
      index++
    }

    if (index < array.length) {
      console.log(`Processed ${index} elements so far...`)
      // Запланировать следующий чанк сразу после текущего
      setTimeout(processChunk, 1000)
    } else {
      console.log(`Completed processing ${array.length} elements in ${Date.now() - startTime} ms`)
    }
  }

  processChunk()
}

// Создаём большой массив
const largeArray = new Array(1000000).fill(0)

// Запускаем обработку массива, ограничивая время выполнения подзадачи 17 миллисекундами
processArrayInChunks(largeArray, 17)
```

Что здесь происходит?

- **`processArrayInChunks`** принимает массив и время обработки каждой подзадачи. Она инициирует процесс разделения задачи на части.
- **`processChunk`** вызывается рекурсивно через **`setTimeout`**. Она обрабатывает часть массива за раз, ограничивая время выполнения заданной длительностью (**`chunkProcessingTime`** ).
- **Условие выхода** из цикла определяется, когда текущий индекс достигает конца массива. После завершения выводится общее время обработки.

💡 Для того чтобы замедлить выполнение задачи, пришлось запустить `setTimeout` с интервалом в секунду.

Благодаря особенности с одной макротаской за раз, получили выполнение фоновой задачи, которая не блокирует страницу.

Такая же логика применяется к браузерным событиям. Разработчик подписывается на событие, и очевидно, что он изменяет состояние DOM. После изменений процесс браузера освободится, например, случится рендеринг, и дальше он возьмёт следующую макротаску.

💡 Кстати, да, браузерные UI-события — это не что иное, как макротаски.

Давайте подведём промежуточный итог. Макротаски — это механизм браузера, предназначеный для «ручного» неблокирующего выполнения **больших** задач. Говоря больших, имею ввиду взять большую задачу и разбить её на мелкие подзадачи.

Теперь настаёт очередь метода `requestAnimationFrame`. Он призван оптимизировать рендеринг и избавить разработчиков от выполнения кастомных сложных анимаций с помощью Timer API. Основная идея тут в том, что попросим браузер запланировать визуальное изменение в ближайшем кадре. Не конкретно одно изменение, а все `rAF`, которые возможно выполнить в следующем кадре. Давайте рассмотрим на примере:

```javascript
let lastTimestamp = Date.now()

function heavyTask() {
  const start = Date.now()

  const workloadPeriod = Math.random() * 10

  while (Date.now() - start < workloadPeriod) {
    // Искусственная загрузка
  }
  console.log('Тяжёлая таска завершена!')
}

let frame = 0
const runFrame = () => {
  frame++
  console.log('кадр', frame, Date.now() - lastTimestamp)

  lastTimestamp = Date.now()

  if (frame < 10) {
    requestAnimationFrame(runFrame)
  }
}

requestAnimationFrame(runFrame)

for (let i = 0; i < 50; i++) {
  setTimeout(() => {
    requestAnimationFrame(() => {
      console.log('раф из макротаски', Date.now() - lastTimestamp)
    })
    heavyTask()
  }, 0)
}
```

Проясним, что делает код. Мы запускаем rAF, который не загружен для лога кадров. Ожидается, что он выполняется каждые 17 миллисекунд. Далее, через цикл, создаём 50 макротасок, которые останавливают цикл событий на отрезке от 0 до 10 миллисекунд. Также выводим в консоль номер кадра и трекаем разницу с предыдущим. Этот пример демонстрирует, как браузер планирует выполнение rAF.

В консоли браузера увидим примерно следующее:

- `кадр 1` — rAF обработан через 2 миллисекунды после начала выполнения кода.
- `Тяжёлая таска завершена!` — выполнено 15 макротасок.
- `кадр 2` — с последнего rAF прошло 104 миллисекунд.
- `раф из макротаски`— вызвано 15 rAF c интервалом в 0 миллисекунд.
- `Тяжёлая таска завершена!` — выполнено 18 макротасок.
- `кадр 3` — с последнего rAF прошло 105 миллисекунд.
- `раф из макротаски` — вызвано 18 rAF c интервалом в 0 миллисекунд.
- `Тяжёлая таска завершена!` — выполнено 16 макротасок.
- `кадр 4` — с последнего rAF прошло 102 миллисекунды.
- `раф из макротаски` — вызвано 16 rAF c интервалом в 0 миллисекунд.
- `Тяжёлая таска завершена!` — выполнено 19 макротасок.
- `кадр 5` — с последнего rAF прошло 102 миллисекунды.
- `раф из макротаски`— вызвано 19 rAF c интервалом в 0 миллисекунд.
- `Тяжёлая таска завершена!` — выполнено 20 макротасок.
- `кадр 6` — с последнего rAF прошло 104 миллисекунды.
- `раф из макротаски` — вызвано 20 rAF c интервалом в 0 миллисекунд.
- `Тяжёлая таска завершена!` — выполнено 12 макротасок.
- `кадр 7` — с последнего rAF прошло 64 миллисекунды.
- `раф из макротаски` — вызвано 12 rAF c интервалом в 0 миллисекунд.
- `кадр 8` — с последнего rAF прошло 0 миллисекунд.
- `кадр 9` — с последнего rAF прошло 13 миллисекунд.
- `кадр 10` — с последнего rAF прошло 17 миллисекунд.

Давайте разберём вывод в консоли. Каждый `setTimeout` вызывает `rAF` и тяжёлую задачу, блокирующую цикл событий. Как только браузер понимает что накопилось достаточно `rAF` для выполнения рендеринга, он пытается применить изменения, которые были запрошены через `rAF` из `setTimeout`. Судя по логу, у браузера получилось выполнить 15–20 микротасок перед тем, как выполнить запросы `rAF`. В среднем браузер планировал накопившиеся вызовы `rAF` каждые 100 миллисекунд.

Какой вывод можем из этого сделать? Во-первых, браузер стремиться выполнить rAF в ближайший кадр (каждые 17 миллисекунд). Во-вторых, браузер планирует как можно больше задач перед следующими вызовами rAF и старается положить в один кадр как можно больше вызовов rAF, инициированные макротасками.

Подчеркнём, что пример иллюстрирует базовые аспекты взаимодействия браузера с `requestAnimationFrame`. На практике вызовы `requestAnimationFrame` включают операции, влияющие на DOM и потребляющие ресурсы процессора. Это может привести к тому, что браузер постарается объединить несколько изменений в один рендеринговый цикл и отложить изменения DOM до следующего кадра для оптимизации производительности.

Дальше, после `requestAnimationFrame`, в стандарте появляются микротаски. Они предназначены для оптимизиции браузерного рендеринга и собирают **как можно больше** изменений состояния приложения. Эти состояния сначала повлияют на DOM, а потом выполнится рендеринг. Дело в том, что процесс рендеринга — дорогая и сложная операция для браузера. Разработчики активно пытаются оптимизировать рендеринг, поэтому в стандарте появляются дополнительные возможности событийного цикла, например, rAF и концепция микротасок.

Запустим этот код и докажем, что рендеринг выполнится после выполнения всех макротасок. 

```javascript
Promise.resolve('Данные загружены').then((message) => {
  console.log(message)
  requestAnimationFrame((timestamp) => {
    console.log('Анимация для:',message, timestamp)
  })
})

Promise.resolve('Настройки загружены').then((message) => {
  console.log(message)
  requestAnimationFrame((timestamp) => {
    console.log('Анимация для:',message, timestamp)
  })
})

Promise.resolve('Данные пользователя загружены').then((message) => {
  console.log(message)
  requestAnimationFrame((timestamp) => {
    console.log('Анимация для:' ,message, timestamp)
  })
})
```

Вывод будет таким:

1. `Данные загружены`.
1. `Настройки загружены`.
1. `Данные пользователя загружены`.
1. `Анимация для: Данные загружены 9516312.2`.
1. `Анимация для: Настройки загружены 9516312.2`.
1. `Анимация для: Данные пользователя загружены 9516312.2`.

Можно заметить, все вызовы rAF выполнились после микротасок, и у них одинаковый `timestamp`. Это говорит о том, что **в нашем случае** браузер запланировал анимацию (сопутствующие изменения в DOM) в ближайший цикл обновления страницы.

Ещё раз подсветим различия между микро- и макротасками:

- браузер выполняет все микротаски подряд, а потом переходит к рендерингу и одной макротаске.
- микротаски выполняются без каких либо задержек, как, например, это может происходить у Timer API (минимальная задержка в 4 миллисекунды).
- принцип выполнения макротасок — одна занимающая процесс макротаска, после её выполнения событийный цикл должен быть разблокирован для других задач. Только после этого будет взята следующая макротаска если конечно же она есть.
- у микротасок гарантированный порядок исполнения. То есть, если где-то в коде отложите задачи с помощью микротасок, они будут выполнены только в порядке создания. У вас может возникнуть вопрос, а что с Fetch API? `fetch` — это же промис, значит, микротаска. Он должен выполняться согласно порядку вызова микротасок! Когда используете `fetch` для выполнения HTTP-запроса, он работает асинхронно, возвращая промис. Этот промис сам по себе не микротаска, но он будет добавлен в очередь микротасок после того, как запрос завершится. Будет получен результат или запрос завершится с ошибкой.

```javascript
console.log('Начало скрипта')

// Асинхронный запрос через fetch
fetch('https://jsonplaceholder.typicode.com/todos/1')
  .then(response => response.json())
  .then(data => {
    console.log('Данные получены:', data)
  })
  .catch(error => {
    console.error('Ошибка при запросе:', error)
  })

  // Добавление микротаски
  queueMicrotask(() => {
    console.log('Выполнение микротаски')
  })

  // Установка таймера с задержкой 0 миллисекунд
  setTimeout(() => {
    console.log('Выполнение макротаски (setTimeout)')
  }, 0)

  console.log('Конец скрипта')
```

Порядок вывода:

1. `Начало скрипта`.
1. `Конец скрипта`.
1. `Вызов fetch`.
1. `Выполнение микротаски (queueMicrotask)`.
1. `Выполнение макротаски (setTimeout)`.
1. `Микротаска then(response => response.json())`.
1. `Микротаска then(data => ...)`.
1. `Микротаска catch(error => ...)`.

Здесь заметитим что, не смотря на вызов `fetch` раньше макротаски, результат запроса обработан только после выполнения задачи.

## Выводы

В этой статье мы рассмотрели различия макро- и микротасок, а также слегка коснулись рендеринга страницы. Понимание различий задач и их приоритетов помогает разработчикам создавать более производительные, отзывчивые и надёжные веб-приложения, а ещё эффективно управлять асинхронными операциями и событиями.
