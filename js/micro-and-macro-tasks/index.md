---
title: "Микротаски и Макротаски подброное объяснение"
description: "Понимание микротасок и макротасок — ключ к созданию оптимизированных и высокопроизводительных приложений"
authors:
  - marsel
tags:
  - article
---

# Микротаски и макротаски

## Event Loop

Вспомним, как функционирует цикл событий в браузере. Event Loop состоит из трёх основных частей:

1. Движок JavaScript -- компонент, который выполняет код JavaScript и обеспечивает доступ к Web API.
2. Web API -- это интерфейсы браузера, такие как fetch, таймеры и другие функции.
3. Очередь событий -- место, где накапливаются события пользовательского интерфейса и события, инициированные Web API, например результат выполнения fetch.

![Основные участники Event Loop](./images/runtime.png)

Работает он следующим образом:

1. **Выполнение кода**: Сначала выполняется синхронный код в стеке вызовов, пока стэк **(Stack)** не опустеет.
2. **Проверка очереди событий** : Когда стек вызовов опустеет, Event Loop проверяет очередь событий. Например завершился `fetch` или мы подписались на какой нибудь UI Event типа `click`и это событие было выброшено.
3. **Перемещение событий в стек**: Если в очереди **(Queue)** событий есть ожидающие задачи, Event Loop переносит их в стек вызовов для выполнения.
4. **Повторение** : Процесс продолжается циклически, обрабатывая события и выполняя асинхронные задачи.

Для большего понимания давайте рассмотрим как это работает на схеме ниже

![Пример обработки задачи в Event Loop](./images/event-loop-example.png)

1. Сначала мы выполняем запрос `fetch`
2. Из стэка эта задача помещается в Web API.
3. Далее браузер выполняет запрос и возвращает результат выполнения в очередь, в каком-то своём внутреннем представлении
4. Далее Event Loop помещает результат выполнения `fetch` в стэк, где его обрабатывает JS движок.

> 💡 Будьте внимательны Event Loop не является частью движка JavaScript

## Микротаски и макротаски

Перед тем как разбираться с примерами кода, давайте разберёмся что такое микротаска и макротаска:

- **Микротаски** это отложенные задачи которые имеют приоритет над макротасками. Примеры микротасок:

  - Операции с промисами (например, `Promise.then()`, `Promise.catch()`, `fetch` и т.д.)
  - Операции с очередью мутации (например, используемые в API `MutationObserver` для наблюдения за изменениями DOM)
  - Операции, связанные с `queueMicrotask()` -- функцией для явного добавления микрозадач

- **Макротаски** это так же отложенные задачи, которые имеют меньший приоритет в сравнении с микротасками

  - Обработка таймеров (`setTimeout`, `setInterval`)
  - Обработка событий пользовательского ввода (например, клики, скроллинг)
  - Выполнение AJAX-запросов

## Асинхронность в браузере

Давайте рассмотрим два вида асинхронного поведения доступного в браузере:

1. **Асинхронность WebAPI.** Давайте рассмотрим как будет работать `fetch`. Мы выполняем `fetch` запрос, точнее движок JS выполняет этот вызов, далее вызов передаётся на сторону Web API, а основной поток браузера продолжает работу. После того как fetch запрос завершиться, Web API вернёт результат выполнения в очередь, далее этот результат выполнения будет подхвачен EventLoop и обработан JS движком.
2. **Асинхронность JS кода** (псевдоасинхронность). Если мы возьмём фукнцию, которая выполняет цикл от 0 до 1000000000, завернём её в промис, то как вы думаете что произойдёт? Правильно, браузер зависнет. Но почему? Дело в том что оборачивая код в промис мы лишь просим браузер отложить выполнение этого кода, на момент пока очередь браузера не будет обработана. Оборачивание синхронного кода в `Promise` или запуск его через `queueMickotask` лишь помещает наш код в почти конец очереди. Почему почти? Мы разберёмся с этим далее.

> 💡 Есть ли способ, выполнить JS код асинхронно и в отдельном потоке браузера? Да! Это возможно с помощью Web Worker API. Прочитать про них можно [тут](https://doka.guide/js/web-workers)

## **Пример с макротасками**

Макротаски включают в себя задачи, такие как таймеры ( **`setTimeout`**, **`setInterval`** ), и они запланированы на выполнение одна за другой.

```typescript
console.log('Начало');

setTimeout(() => {
  console.log('Макротаска выполнена');
}, 0);

console.log('Конец');
```

В этом примере порядок вывода будет следующим:

1. `Начало`
2. `Конец`
3. `Макротаска выполнена`

Даже если `setTimeout` имеет задержку в 0 миллисекунд, его колбэк не выполняется немедленно, а планируется как макротаска и выполняется в конце приоритета выполнения.

## **Пример с микротасками**

Микротаски включают в себя обещания (Promises) и обработчики, связанные с ними.

```typescript
console.log('Начало');

Promise.resolve().then(() => {
  console.log('Микротаска выполнена');
});

console.log('Конец');
```

Здесь порядок выполнения будет таким:

1. `Начало`
2. `Конец`
3. `Микротаска выполнена`

Поскольку микротаски имеют более высокий приоритет, чем макротаски, колбэк обещания (Promise) выполняется сразу после завершения текущего цикла выполнения, но перед следующим циклом событий, даже если в Event Loop уже есть макротаски.

## **Комбинированный пример: Микротаска + Макротаска**

Давайте посмотрим на комбинированный пример, который включает в себя и микротаски, и макротаски:

```typescript
console.log('Начало');

setTimeout(() => {
  console.log('Макротаска выполнена');
}, 0);

Promise.resolve().then(() => {
  console.log('Микротаска выполнена');
});

Promise.resolve().then(() => {
  console.log('Микротаска выполнена 2');
});

console.log('Конец');
```

Порядок выполнения будет следующим:

1. `Начало`
2. `Конец`
3. `Микротаска выполнена`
4. `Микротаска выполнена 2`
5. `Макротаска выполнена`

Этот пример показывает, что микротаски (здесь, промисы) выполняются перед макротасками (здесь, таймеры), даже если они были запланированы позже в коде. Это подчёркивает их приоритет в Event Loop.

> 💡 Обратите внимание на приоритеты. Сначала будут выполнены **все** микротаски, а потом будет взята **одна** макротаска.

## Пример с рендерингом страницы

Я немного упустил детали, когда написал, что после всех микротасок выполняется одна макротаска. На самом деле, после микротасок может произойти рендеринг страницы. Если вы используете `requestAnimationFrame` , эти вызовы могут быть выполнены до того, как начнётся выполнение макротаски. Браузер старается вставить `requestAnimationFrame`

в ближайший рендеринг. Все вызовы `requestAnimationFrame` пытаются происходить примерно каждые 16, 67 мс, но **строго** после завершения всех микротасок. Давайте посмотрим на пример ниже:

```typescript
console.log("Синхронный старт");

// Макротаска с setTimeout
setTimeout(() => {
  console.log("Макротаска 1: setTimeout");
}, 0);

// Макротаска с setTimeout
setTimeout(() => {
  console.log("Макротаска 2: setTimeout");
}, 0);

// Микротаска с Promise
Promise.resolve()
  .then(() => {
    console.log("Микротаска 1: выполнение первого промиса");
  })
  .then(() => {
    console.log("Микротаска 2: выполнение второго промиса");
  });

// Первый requestAnimationFrame для обновления анимации
requestAnimationFrame(() => {
  console.log("Первый requestAnimationFrame: обновление анимации");
});

// Второй requestAnimationFrame для обновления анимации
requestAnimationFrame(() => {
  console.log("Второй requestAnimationFrame: обновление анимации");
});

console.log("Синхронный конец");
```

Порядок вывода будет следующим:

1. `Синхронный старт`
2. `Синхронный конец`
3. `Микротаска 1: выполнение первого промиса`
4. `Микротаска 2: выполнение второго промиса`
5. `Первый requestAnimationFrame: обновление анимации`
6. `Второй requestAnimationFrame: обновление анимации`
7. `Макротаска 1: setTimeout`
8. `Макротаска 2: setTimeout`

Нам повезло и rAF вызовы выполнились перед макротасками. Если мы выполним этот код ещё раз, то порядок rAF и микротаски могут поменяться местами.

## Пример с макротаской вызывающей микротаску

```typescript
console.log('Начало скрипта');

setTimeout(() => {
  console.log('Макротаска: setTimeout');

  // Микротаска, созданная внутри макротаски
  Promise.resolve().then(() => {
    console.log('Микротаска: обработка промиса внутри setTimeout');
  });
}, 0);

Promise.resolve().then(() => {
  console.log('Микротаска: обработка первого промиса');
});

console.log('Конец скрипта');
```

Последовательность вывода будет следующей:

1. `Начало скрипта`
2. `Конец скрипта`
3. `Микротаска: обработка первого промиса`
4. `Макротаска: setTimeout`
5. `Микротаска: обработка промиса внутри setTimeout`

Здесь стоит обратить внимание на ситуацию когда микротаска создаётся в макротаске. Пока макротаска не выполнит свой колбэк, браузер ничего не будет знать о том что там внутри микротаска.

## **Контексты цикла событий**

В этом разделе хочу акцентировать внимание на изолированных контекстах с собственными циклами обработки событий:

- Основная страница браузера
- Iframe
- Web Worker

Каждый из этих элементов обладает своим уникальным циклом событий, который не пересекается с другими. С Web Workers всё довольно прямолинейно: задача отправляется в воркер, и процесс обработки происходит независимо от основного потока браузера.

Однако ситуация с Iframe немного сложнее. **Хотя Iframe и обладает собственным циклом событий, он не имеет отдельного потока выполнения.** Тем не менее, это не означает, что затратная задача в Iframe вызовет зависание основной вкладки браузера, зависнет только iframe а основная вкладка продолжит работу, хотя и браузер будет уведомлять пользователя о том что, что-то подвисло.

## Философия микротасок и макротасок

**Макротаски**
До 2015 года стандарт не подразумевал микротаски, а до 2014 не подразумевал Web Workers. А requestAnimationFrame появился только в 2011 году. Разработчикам приходилось довольствоваться асинхронностью основанной на макротасках или на Timer API. Учитывая всё это макротаски являлись и являются очень мощным инструментом.

Итого сетап такой, ни макротасок, ни raf, ни Web Workers и никакого праздника. Представим ситуацию когда вам нужно посчитать что-то сложное на клиенте или например вы хотите делать кастомные анимации которые невозможно сделать через CSS, но нужно это сделать так чтобы окно браузера не блокировалось но при этом задача выполнялась. Многопоточности в браузере нет есть только однопоточный EventLoop. Как мы можем выйти из ситуации? Правильно! Разбить задачу на много подзадачек и выполнить их постепенно освобождая процесс для рендеринга, эвентов и запросов.

Давайте рассмотрим пример:

```ts
function processArrayInChunks(array, chunkProcessingTime) {
  let index = 0;
  const startTime = Date.now();

  function processChunk() {
    const chunkStartTime = Date.now();

    while (index < array.length && (Date.now() chunkStartTime < chunkProcessingTime)) {
            // Пример обработки: увеличиваем каждый элемент массива на 1
      array[index] += 1;
      index++;
    }

    if (index < array.length) {
      console.log(`Processed ${index} elements so far...`);
      setTimeout(processChunk, 1000); // Запланировать следующий чанк сразу после текущего
    } else {
      console.log(`Completed processing ${array.length} elements in ${Date.now() - startTime} ms`);
    }
  }

  processChunk();
}

// Создаем большой массив
const largeArray = new Array(1000000).fill(0);

// Запускаем обработку массива, ограничивая время выполнения подзадачи 17 миллисекундами
processArrayInChunks(largeArray, 17);
```

Что здесь происходит?

- **`processArrayInChunks`** принимает массив и время обработки каждой подзадачи. Она инициирует процесс разделения задачи на части.
- **`processChunk`** вызывается рекурсивно через **`setTimeout`**. Она обрабатывает часть массива за раз, ограничивая время выполнения заданной длительностью (**`chunkProcessingTime`** ).
- **Условие выхода** из цикла определяется, когда текущий индекс достигает конца массива. После завершения выводится общее время обработки.

> 💡 Для того чтобы замедлить выполнение задачи, пришлось запускать setTimeout с интервалом в секунду.

Благодаря особенности: одна макротаска за раз, мы получили неблокирующее страницу выполнение фоновой задачи.

Абсолютна такая же логика применяется к браузерным эвентам. Разбаботчик подписывается на какой-то эвент, очевидно что он будет менять состояние дом, после изменений процесс браузера освободится, например случится рендеринг, и дальше возьмёт следующую макротаску.

> 💡 Кстати, да, браузерные UI эвенты это не что иное как макротаски.

Давайте подведём итог. Макротаски это мощный механизм браузера, предназначеный для "ручного" неблокирующего выполнения **больших** задач. Говоря больших я имею ввиду взять большую задачу и разбить её на мелкие подзадачи.

**requestAnimationFrame** Стандарт развивается и в нём появляется `requestAnimationFrame` призванный оптимизировать рендеринг и избавить разработчиков от выполнения кастомных сложных анимаций с помощью Timer API.

Его основная идея, попросить браузер запланировать визуальное изменение в ближайший кадр. Не конкретно одно изменение, а все `rAF` которые возможно выполнить в следующим кадре. Давайте рассмотрим на примере:

```ts
let lastTimestamp = Date.now();

function heavyTask() {
  const start = Date.now();

  const workloadPeriod = Math.random() * 10;

  while (Date.now() - start < workloadPeriod) {
    // Искусственная загрузка
  }
  console.log("Тяжёлая таска завершена!");
}

let frame = 0;
const runFrame = () => {
  frame++;
  console.log("кадр", frame, Date.now() - lastTimestamp);

  lastTimestamp = Date.now();

  if (frame < 10) {
    requestAnimationFrame(runFrame);
  }
};

requestAnimationFrame(runFrame);

for (let i = 0; i < 50; i++) {
  setTimeout(() => {
    requestAnimationFrame(() => {
      console.log("раф из макротаски", Date.now() - lastTimestamp);
    });
    heavyTask();
  }, 0);
}
```

Давайте для начала проясним что делает этот код. Мы запускаем наш rAF, который никак не загружен для лога кадров, в идеале ожидается что он выполняется каждые 17мс. Далее через цикл, мы создаём 50 макротасок которые стопят наш цикл событий на от 0 до 10ms. Так же мы выводим в консоль номер кадра и трекаем разницу с предыдущим кадром. Это пример нам нужен чтобы продемонстрировать как браузер планирует выполнение rAF.

В консоли браузера мы увидим примерно следующее:

- `кадр 1` - rAF был обработан через 2мс после начала выполения нашего кода

- `Тяжёлая таска завершена!` - выполнено 15 макротасок

- `кадр 2` - с последнего rAF прошло 104мс

- `раф из макротаски`- вызвано 15 rAF c интервалом в 0мс

- `Тяжёлая таска завершена!` - выполнено 18 макротасок

- `кадр 3` с последнего rAF прошло 105мс

- `раф из макротаски`- вызвано 18 rAF c интервалом в 0мс

- `Тяжёлая таска завершена!` - выполнено 16 макротасок

- `кадр 4` с последнего rAF прошло 102мс

- `раф из макротаски`- вызвано 16 rAF c интервалом в 0мс

- `Тяжёлая таска завершена!` - выполнено 19 макротасок

- `кадр 5` с последнего rAF прошло 102мс

- `раф из макротаски`- вызвано 19 rAF c интервалом в 0мс

- `Тяжёлая таска завершена!` - выполнено 20 макротасок

- `кадр 6` с последнего rAF прошло 104мс

- `раф из макротаски`- вызвано 20 rAF c интервалом в 0мс

- `Тяжёлая таска завершена!` - выполнено 12 макротасок

- `кадр 7` с последнего rAF прошло 64мс

- `раф из макротаски`- вызвано 12 rAF c интервалом в 0мс

- `кадр 8` с последнего rAF прошло 0мс

- `кадр 9` с последнего rAF прошло 13мс

- `кадр 10` с последнего rAF прошло 17мс

Давайте немного объясню вывод консоли. Каждый `setTimeout` вызывает `rAF` и тяжёлую задачку блокирующую цикл событий. Как только браузер понимает что накопилось достаточно `rAF` для того чтобы выполнить рендеринг, браузер пытается применить изменения, которые были запрошены через `rAF` из `setTimeout` . Если судить по логу то в у браузера получалось выполнить 15-20 микротасок, перед тем как выполнить запросы `rAF` и в среднем браузер планировал накопившиеся вызовы `rAF` каждые 100мс

Какой вывод мы можем из этого сделать? Во-первых браузер стремиться выполнить rAF в ближайший кадр (имеется ввиду каждые 17мс), во-вторых мы наглядно видим что браузер пытается запланировать как можно больше задач перед следующими вызовами rAF и **постараться** как можно больше вызово rAF иницированные макротасками положить в один кадр.

> 💡 Следует подчеркнуть, что данный пример иллюстрирует базовые аспекты взаимодействия браузера с `requestAnimationFrame` . В практическом применении вызовы `requestAnimationFrame` включают в себя операции, влияющие на DOM и потребляющие ресурсы процессора. Это может привести к тому, что браузер будет стремиться объединить несколько изменений в один рендеринговый цикл, но так же может отложить определённые изменения DOM до следующего кадра для оптимизации производительности.

**Микротаски** Далее по таймлайну в стандарте появляются микротаски предназначеные для того чтобы оптизимировать процесс рендеринга браузера и **собрать как можно больше изменений состояния приложения,** которые в последующем повлияют на DOM **и только после этого выполнить рендеринг** .

> 💡 Дело в том что процесс рендеринга браузерами это очень дорогая и сложная операция. Разработчики браузеров пытаются оптимизировать её как можно сильнее для этого в стандарте появляются дополнительные акторы событийного цикла как например rAF и концепция микротасок.

Для того чтобы доказать что рендеринг выполнится после выполнения всех макротасок, давайте запустим код ниже.

```typescript
Promise.resolve("Данные загружены").then((message) => {
  console.log(message);
  requestAnimationFrame((timestamp) => {
    console.log('Анимация для:',message, timestamp);
  });
});

Promise.resolve("Настройки загружены").then((message) => {
  console.log(message);
  requestAnimationFrame((timestamp) => {
    console.log('Анимация для:',message, timestamp);
  });
});

Promise.resolve("Данные пользовтеля загружены").then((message) => {
  console.log(message);
  requestAnimationFrame((timestamp) => {
    console.log('Анимация для:' ,message, timestamp);
  });
});
```

Вывод будет следующий:
1. `Данные загружены`
2. `Настройки загружены`
3. `Данные пользователя загружены`
4. `Анимация для: Данные загружены 9516312.2`
5. `Анимация для: Настройки загружены 9516312.2`
6. `Анимация для: Данные пользовтеля загружены 9516312.2`

Как можно заметить все вызовы rAF выполнились после микротасок, а так же имеют одинаковый timestamp. Это говорит нам о том **что в нашем случае** , браузер запланировал анимацию (сопутствующие изменения в DOM), в ближайший цикл обновления страницы.

В качестве итога этого блока хочется ещё раз подсветить различия микротасок и макротасок

- браузер выполняет все микротаски подряд, а потом переходит к рендерингу и одной макротаске
- микротаски выполняются без каких либо задержек, как например это может происходить у Timer API (минимальная задержка в 4мс)
- макротаски выполняются по принципу одна занимающая процесс макротаска, после выполнения которой событийный цикл должен быть разблокирован для других задач и только после этого будет взята следующая макротаска если конечно же она есть.
- микротаски имеют гарантированный порядок исполнения. То есть, если в течении кода вы решите отложить какие-то задачи с помощью микротасок, то они будут выполнены ровно в таком порядке, в каком они были созданы. У вас может возникнуть вопрос, а что с `fetch API`? Fetch это же промис, значит микротаска, значит должен выполняться согласно порядку вызова микротасок! Когда вы используете `fetch` для выполнения HTTP-запроса, он работает асинхронно, возвращая промис. Этот промис не является микротаской сам по себе, но он будет добавлен в очередь микротасок после того, как запрос завершится (результат будет получен или запрос завершится с ошибкой).

```ts
console.log('Начало скрипта');

// Асинхронный запрос через fetch
fetch('https://jsonplaceholder.typicode.com/todos/1')
  .then(response => response.json())
  .then(data => {
    console.log('Данные получены:', data);
  })
  .catch(error => {
    console.error('Ошибка при запросе:', error);
  });

  // Добавление микротаски
  queueMicrotask(() => {
    console.log('Выполнение микротаски');
  });

  // Установка таймера с задержкой 0 миллисекунд
  setTimeout(() => {
    console.log('Выполнение макротаски (setTimeout)');
  }, 0);

  console.log('Конец скрипта');
```

1. `Начало скрипта`
2. `Конец скрипта`
3. `Вызов fetch`
4. `Выполнение микротаски (queueMicrotask)`
5. `Выполнение макротаски (setTimeout)`
6. `Микротаска then(response => response.json())`
7. `Микротаска then(data => ...)`
8. `Микротаска catch(error => ...)`

Здесь мы можем заметить что не смотря на то что fetch был вызван раньше макротаски, результат запроса был обработан после выполнения макротаски

## Итог
В этой статье мы с вами рассмотрели различия макротасок и микротасок, а так же немного коснулись рендеринга страницы. Понимание различий задач и их приоритетов помогает разработчикам создавать более производительные, отзывчивые и надёжные веб-приложения, а также эффективно управлять асинхронными операциями и событиями.
